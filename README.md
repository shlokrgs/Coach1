
# ALIGN Frontend (React + Vercel)

This is the frontend for the ALIGN Coaching Platform. Deploy it on Vercel using these settings:

- **Build Command**: `npm install && npm run build`
- **Output Directory**: `build`

To connect it to your FastAPI backend on Railway, update the API base URL in your React code.
